// tests/unit/stub-server-only.ts
// Pengganti paket `server-only` khusus saat unit test (lihat resolve.alias di vitest.config.ts).
// Sengaja kosong: yang dibutuhkan hanya "impor ini tidak melempar galat".
export {}
